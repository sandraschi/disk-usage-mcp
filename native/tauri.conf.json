{
  "productName": "Disk Usage MCP",
  "version": "0.1.0",
  "identifier": "ai.fleet.disk-usage-mcp",
  "build": {
    "frontendDist": "../web_sota/dist",
    "devUrl": "http://localhost:11115",
    "beforeDevCommand": "pwsh -NoProfile -Command \"$env:PATH=\"$env:USERPROFILE\\.bun\\bin;\"+$env:PATH; Set-Location ..\\web_sota; bun run dev\"",
    "beforeBuildCommand": "pwsh -NoProfile -Command \"$env:PATH=\"$env:USERPROFILE\\.bun\\bin;\"+$env:PATH; Set-Location ..\\web_sota; bun run build\""
  },
  "app": {
    "windows": [{
      "label": "main",
      "title": "Disk Usage MCP",
      "width": 1200,
      "height": 800,
      "minWidth": 800,
      "minHeight": 600
    }],
    "security": { "csp": null }
  },
  "bundle": {
    "active": true,
    "targets": ["nsis"],
    "icon": ["icons/icon.ico", "icons/icon.png"],
    "resources": [
      "resources/disk-usage-mcp-backend.exe",
      "resources/.env.example"
    ],
    "windows": {
      "webviewInstallMode": { "type": "skip" },
      "nsis": {
        "installMode": "currentUser",
        "installerHooks": "./windows/hooks.nsh"
      }
    }
  }
}
